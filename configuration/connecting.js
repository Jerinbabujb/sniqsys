// index.js
/*
const connection = require('./connection');

module.exports.connect=connection.connect(function(err) {
  if (err) {
    console.log(err);
    return;
  }

  // The database connection is now available in the `mongoose` object
  // ...
  console.log('Connected to the database');
});*/
